# Smart Coupling API

Run from this directory:

```powershell
pip install -r requirements.txt
uvicorn main:app --reload
```

The first start creates `database/couplings.db` and seeds the ten coupling types.
